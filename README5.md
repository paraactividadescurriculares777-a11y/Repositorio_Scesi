#Trabajo individual
Dana Sainz Rubin de Celis 
##Clase 5
###Ramas:Bifurcacion que tiene exactamente el mismo codigo (una copia)
```
git Branch
git branch FeatureExample
git swith main
git checkout nombre-rama
gitflow
git init
echo "Landing page" >>README.md
rm scrips.js
```
####Ejemplo de buen commit:
git commit -m "feat:add funtion sumar in script.js"
###Ramas Feature:
 caracteristica nueva en nuestra
(todo lo que vaya ha hacer lo trabajo en otra rama)
no usar espacios en flow y despues de cambiar la rama a principal la borramos (initial commit)
####Rama de apoyo release
```
git checkout -b releaseqa-sum-function
```
ramas en develop
crear las ramas cuando estamos añadiendo nuevas funciones
head es en donde estas y main es hasta donde subiste al repositorio remoto

